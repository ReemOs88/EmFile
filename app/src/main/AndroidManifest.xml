<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.emfile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EmFile">
        <activity android:name=".VaccinationDate"></activity>
        <activity android:name=".BookDoctorAppointment"></activity>
        <activity android:name=".EditMedicalReport" ></activity>
        <activity android:name=".MedicalReport"></activity>
        <activity android:name=".PatientHome" ></activity>
        <activity android:name=".HealthInformation" ></activity>
        <activity android:name=".PatientRegister" ></activity>
        <activity android:name=".PatientLogin" ></activity>
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>